<%= form_for(@video) do |f| %>
  <% if @video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
      <% @video.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "视频简介" %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label "视频地址" %><br />
    <%= f.text_area :url ,size:"10x10"%>
  </div>
  <div class="actions">
    <%= f.submit "确定上传"%>
  </div>
<% end %>
