<%= form_for(@shuoshuo) do |f| %>
  <% if @shuoshuo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shuoshuo.errors.count, "error") %> prohibited this shuoshuo from being saved:</h2>

      <ul>
      <% @shuoshuo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "标题" %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label "内容" %><br />
    <%= f.text_field :content %>
  </div>

  <div class="actions">
    <%= f.submit "确定修改"%>
  </div>
<% end %>
